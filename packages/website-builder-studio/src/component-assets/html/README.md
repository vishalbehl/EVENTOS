These HTML files are static companion artifacts for previews, import/export, and design review.

The editable source of truth is the JSON/component asset registry because it can preserve component identity, child structure, settings targets, links, migrations, and data bindings.
