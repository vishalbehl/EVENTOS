"use client";

// ThemeSwitcher has been deprecated in favour of a simple dark/light toggle
// built into the Sidebar and Header components.
// This export is kept for backward compatibility with any existing imports.

export function ThemeSwitcher({ className }: { className?: string }) {
  return null;
}
