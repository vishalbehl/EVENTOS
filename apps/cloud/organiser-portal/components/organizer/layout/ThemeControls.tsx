"use client";

// ThemeControls has been deprecated — the dark/light toggle 
// is now built into the Sidebar and Header components.
// This file is intentionally left as a no-op export for backward compatibility.

export function ThemeControls() {
  return null;
}
